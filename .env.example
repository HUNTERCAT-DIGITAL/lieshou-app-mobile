# ============================================================
# LieShouCloud Mobile · 环境变量（Expo 构建期内联）
# 复制为 .env 使用（apps/mobile/.env）：
#   cp .env.example .env
# 也可在 EAS Build 的 environment variables 或 CI 中注入。
#
# EXPO_PUBLIC_ 前缀变量会被 Expo CLI 内联到 JS bundle（非敏感，勿放密钥）。
# ============================================================

# 后端 API 基址（构建期注入；未设置时默认 dev 域名）
#  - Expo Go 联调：https://expo.lieshoucloud.huntercat.cn（Metro 代理）
#  - 正式 APK：生产域名（如 https://api.lieshoucloud.huntercat.cn，待定）
EXPO_PUBLIC_API_BASE=https://expo.lieshoucloud.huntercat.cn
